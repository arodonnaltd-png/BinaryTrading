import React from "react";
import ReactDOM from "react-dom/client";
import AromeTerminal from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AromeTerminal />
  </React.StrictMode>
);
