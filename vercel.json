{
  "buildCommand": "echo 'Static site with serverless API'",
  "outputDirectory": ".",
  "cleanUrls": false,
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1",
      "methods": ["GET", "POST", "OPTIONS"]
    },
    {
      "src": "/(.*)",
      "dest": "/arome-binary-signal.html"
    }
  ]
}
